---
version: 2.1

jobs:
  urlcheck:
    docker:
      - image: circleci/buildpack-deps:focal-scm
    environment:
      - OCPN_TARGET: focal
    steps:
      - checkout
      - run: tools/ci-check
workflows:
  version: 2
  build_all:
    jobs:
      - urlcheck
